package com.gabeochoa;

public class MathBackup {

	
	/*
	 * This will hold all the methods that I used before switching, as a backup so that I dont lose it.
	 * 
	
	
	
	 private double getVelocityX() {
		 double dy = (player.getCenterY() - mouseY);
		 double dx = (mouseX - player.getCenterX());
		 
		 double angle = Math.atan(dy/dx);
//	 			angle -= Math.PI /2;
		
			 return  Math.cos(angle);
	 }
		 
	private double getVelocityY() {
			 double dy =  (player.getCenterY() - mouseY);
			 double dx =  (mouseX - player.getCenterX());
			 
			 double angle = Math.atan(dy/dx);
			 angle -= Math.PI /2;
			 
			 
			 return  Math.sin(angle);
		
	}
	






















	*/
}
